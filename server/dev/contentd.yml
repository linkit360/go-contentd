metric_instance_prefix: pk_mobilink1
app_name: contentd

server:
  rpc_port: 50301
  http_port: 50302

service:
  retry_count: 10

inmem_client:
  timeout: 10

notifier:
  queues:
    content_sent: content_sent
  rbmq:
    conn:
      user: linkit
      pass: dg-U_oHhy7-
      host: localhost
      port: 5672
    chan_capacity: 10000
    pending_buffer_path: /home/centos/linkit/contentd_pending.json
